################################################################
Bugs
################################################################
- it is hard to switch languages in content panel and to select languages in attribute panel
- if duplicate wrapper blocks, the layout picker of the main wrapper will not work (all duplicate blocks will go to the same point when clicking on the picker)
- if enable hidding appearance popover on mouseleave, then can not open the popover for border property
- chosen control seems not similar to an auto-complete control. We need to keep the focus point as well as insert things into the focus point
- when add Zoom css, returrn error forr unmatch color
